EXPO_PUBLIC_OPENROUTER_API_KEY=
EXPO_PUBLIC_NEWSDATA_API_KEY=
EXPO_PUBLIC_OPENROUTER_MODEL=meta-llama/llama-3.1-8b-instruct:free

# Future wiring point — once the FastAPI backend (per the spec) is deployed,
# point the app at it here and the services/* layer flips from local/direct
# third-party calls to real backend calls with no shape changes.
EXPO_PUBLIC_API_BASE_URL=
