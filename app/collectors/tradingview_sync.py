import asyncio


async def sync_tradingview_indicators() -> None:
    """Placeholder for polling TradingView overlays and updating graph indicators."""
    while True:
        await asyncio.sleep(60)
